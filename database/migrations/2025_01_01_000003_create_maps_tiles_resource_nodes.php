<?php
use Illuminate\Database\Migrations\Migration; use Illuminate\Database\Schema\Blueprint; use Illuminate\Support\Facades\Schema; return new class extends Migration { public function up(): void { Schema::create('maps', function(Blueprint $t){ $t->id(); $t->unsignedBigInteger('game_id'); $t->string('name')->nullable(); $t->json('meta')->nullable(); $t->timestamps(); }); Schema::create('tiles', function(Blueprint $t){ $t->id(); $t->unsignedBigInteger('map_id'); $t->integer('x'); $t->integer('y'); $t->string('terrain')->default('grass'); $t->json('meta')->nullable(); $t->timestamps(); }); Schema::create('resource_nodes', function(Blueprint $t){ $t->id(); $t->unsignedBigInteger('map_id'); $t->string('type'); $t->integer('x'); $t->integer('y'); $t->integer('amount')->default(100); $t->json('meta')->nullable(); $t->timestamps(); }); } public function down(): void { Schema::dropIfExists('resource_nodes'); Schema::dropIfExists('tiles'); Schema::dropIfExists('maps'); } };
